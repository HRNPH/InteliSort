version: '3'
services:
  redis:
    image: redis
    container_name: siph-asr-redis
    ports:
      - '6379:6379'
    restart: always
    env_file:
      - ./.env
    volumes:
      - redis-db-siph-asr:/data
volumes:
  redis-db-siph-asr: